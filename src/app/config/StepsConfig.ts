const StepsConfig = () => {
    const options = {
        nextLabel: '>',
        prevLabel: '<',
        hidePrev: true,
        buttonClass: 'px-3 bgc-primary tc-white fw-lightBold br-corner',
        tooltipClass: 'ff-default'
    };

    return { options };
}

export default StepsConfig;