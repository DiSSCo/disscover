/* API service constants */
const STALE_TIME = 1000 * 60 * 5; // 5 minutes until time is stale
const GC_TIME = 1000 * 60 * 10; // 10 minutes to store data in the cache

export {
    STALE_TIME,
    GC_TIME
}